/* Test 3 */
/* Can you handle the Read(), Fread(), and Write() calls? */

struct node
{
	int length;
	union
	{
		int intvalue;
		float floatvalue;
		union {
			struct bs {
			int b;
			float s;
			} x1;
			int y;
			} xx;
	} value;
} z1,z2,z3;


int main()
{
	/* typedef construct used here */
	typedef int COUNTERS;
	COUNTERS i;

	/* float used here, also the variable name is mixed with digits and 
	underscore*/
	float someReal12345;

	/* int declared here */
	int a,b,n;

	/* Read, Fread, and Write are library functions*/

	write("Input some real number:");
	z1.length = read();
	someReal12345 = fread();

	/* While is used like this */
	a = -200000;
	while( n != 0) 
	{
		a = a/n;
		n = n - 1;
	}

	/* Using all the logical and arithmatic operator */

	if ( someReal12345 <= 120.05) 
	{
		write("It is less") ;
	} 
	else 
	{
		if (a == 1) 
		{
		write("A is 1");
		}
		else
		{
			if ( a || n >= 0) 
			{
				write("A greater than zero");
			} 
			else 
			{
				if (a && n == 0) 
				{
					write("Error in decrementing n");
				}
				else 
				{ 
					if (!(a)) 
					{
						a = 1;
						n = -n;
					}
				}
			}
		} 
	}

	return 0; 
}

float f1(int a, int b)
{

    struct a1 {
	int b[100][100];
	float y;
	int b2[1][2][3];
	} c1;
	c1.b[1][2] = 1;
	c1.y = -100.0;
}
