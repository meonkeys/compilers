/* Test4 */
/* Can you detect and handle syntax errors */

void Matrix()
{
}

int main()
{
	typedef int COUNTERS;
	COUNTERS i,n;

	float someRealnumber12345;
	/* int declared here */
	int a,b;
	n = read();
	someRealnumber12345 = fread();

	a = 200000;
	while( n != 0) 
	{
		a = a/n;
		n = n - 1;
	}

	/* Using all the logical and arithmatic operator */

	if ( someRealnumber12345 > 1200.5) 
	{
                write("Too big");
        };
	if ( someRealnumber12345 <= 120.05) 
	{
		write("It is small");
	} 
	else 
	{
		int a,b,c;
		struct x {
			int a,a1,a2;
			double b;
			int a3[10][10];
		} y1, y2[n];
		if (a == 1 || a < 0 || a < b) 
		{
		write("a is <=1  or a < b");
		}
		else
		{
			if ( a && n >= 0) 
			{
				write("a greater than zero");
				y1.a3[1][3] = 0;
				y2[3].a3[0][0] = a*b/n;
			} 
			else 
			{
				if (a && n == 0) 
				{
					write("Error in decrementing n");
				}
				else 
				{ 
					while (!a) 
					{
						a = -a;
					}
					else    a == 1;
				}
			}
		} 
	}

	return 0; 
}
